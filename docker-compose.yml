services:
  lotto-lab:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
    volumes:
      - ./draws.csv:/app/draws.csv
